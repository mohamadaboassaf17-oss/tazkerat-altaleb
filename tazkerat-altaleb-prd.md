# وثيقة متطلبات المنتج (PRD)
# تذكرة الطالب — النسخة 2.0

> **تاريخ المراجعة:** نسخة محدّثة بعد جلسة التدقيق  
> **الحالة:** جاهزة للتطوير

---

## 1. ملخص المشروع

**تذكرة الطالب** تطبيق SaaS متخصص لطلاب العلم الشرعي لتتبع رحلتهم العلمية. يوفر مكاناً واحداً لتتبع الأقسام والكتب والمحاضرات، وتدوين الملاحظات بروابط داخلية ذكية، مع توليد خريطة معرفية تفاعلية ونظام مراجعة مبرمج (SRS) لتثبيت المحفوظات. يعمل التطبيق بنظام **Offline-first** بالكامل مع مزامنة سحابية تلقائية عبر PWA نقي.

---

## 2. رؤية المنتج ونموذج العمل

### 2.1. الرؤية
> "مرجعك الشخصي الذي يسير معك في رحلة طلب العلم: يتبع إنجازك، يحفظ فوائدك، ويربط علمك بعضه ببعض."

### 2.2. نموذج العمل (PLG — Product-Led Growth)
- التطبيق مجاني بالكامل بجميع ميزاته الأساسية والمتقدمة (الروابط، الخريطة، الـ SRS).
- يقتصر الدفع على استهلاك مساحات تخزين الوسائط (الصور والصوتيات) بعد انتهاء الشهر الأول التجريبي.
- الشهر التجريبي يبدأ من **تاريخ أول رفع وسائط**، لا من تاريخ التسجيل.

---

## 3. هيكل المحتوى العلمي

التسلسل الهرمي الكامل للمحتوى:

```
القسم (category)
  └── الكتاب (book)
        └── الشيخ / المدرّس (lecturer)
                └── المحاضرة (lecture)
```

**مثال تطبيقي:**
```
قسم العقيدة
  └── شرح الأصول الثلاثة
        └── الشيخ صالح الفوزان
                └── المحاضرة الأولى
```

---

## 4. تجربة المستخدم الرئيسية

### 4.1. التسجيل والدخول
- تسجيل الدخول عبر **Google OAuth**.
- تسجيل الدخول عبر **البريد الإلكتروني + كلمة المرور** (مع التحقق من البريد وخيار "نسيت كلمة المرور").
- كلا الخيارين مجانيان عبر Supabase Auth.

### 4.2. الإعداد الأولي (Onboarding & Demo Template)
- عند تسجيل المستخدم لأول مرة، لا يظهر له التطبيق فارغاً — يُشحن حسابه تلقائياً بقالب تجريبي تفاعلي يوضح فلسفة التطبيق.
- **محتوى القالب التجريبي:**
  - قسم: العقيدة
  - كتاب: الأصول الثلاثة
  - شيخ: الشيخ صالح الفوزان
  - محاضرة: المحاضرة الأولى
  - ملاحظة: نموذج **حفظ** مكتوب ومربوط بنظام الـ SRS لتوضيح كيفية عمل الخريطة والمراجعة فوراً
- المستخدم حر في تعديل القالب أو حذفه بالكامل.

### 4.3. تدوين الملاحظات والربط الذكي (`[[`)
- يدعم المحرر ميزة **Wiki-links**: عند كتابة `[[` يفتح صندوق بحث فوري يقترح عناوين الملاحظات الأخرى.
- عنوان الملاحظة **يُستخرج تلقائياً من أول سطر** في المحتوى — لا يكتبه المستخدم يدوياً.
- العنوان المستخرج يُخزَّن في حقل `title` منفصل لأغراض البحث والـ autocomplete والخريطة.
- عند حذف رابط `[[` من النص، يختفي الرابط فوراً من الخريطة — جدول `note_links` يعكس النص بالكامل ويُعاد بناؤه عند كل حفظ.

### 4.4. البحث المرن باللغة العربية
- يعتمد محرك البحث سياسة **تطبيع النصوص (Text Normalization)**:
  - إزالة التشكيل بالكامل.
  - توحيد الهمزات (أ / إ / آ / ا).
  - إسقاط "ال" التعريف والزوائد عند المقارنة.

---

## 5. المتطلبات الوظيفية التفصيلية

### 5.1. لوحة التحكم (Dashboard)

| الكود | المتطلب |
|-------|---------|
| FR-D1 | عرض إحصاءات التقدم ونسب الإنجاز في الأقسام العلمية المختلفة |
| FR-D2 | عرض **خريطة معرفية محلية** تتمحور حول آخر كتاب فُتح أو عُدّلت إحدى ملاحظاته (بناءً على `last_opened_at`) مع زر لفتح الخريطة الكاملة |
| FR-D3 | عرض الملاحظات المستحقة للمراجعة اليومية وأحدث 5 ملاحظات مسجلة |

### 5.2. نظام المراجعة المبرمج (SRS)

| الكود | المتطلب |
|-------|---------|
| FR-S1 | أسلوب "العرض والتأكيد": تظهر الملاحظة كاملة للقراءة، ثم يقيّم الطالب صعوبتها (سهل / متوسط / صعب) |
| FR-S2 | **وضع البطاقات**: جلسة المراجعة تعرض ملاحظة واحدة في كل مرة — لا تظهر التالية إلا بعد تقييم الحالية |
| FR-S3 | خوارزمية التكرار المتباعد مستوحاة من SM-2 |
| FR-S4 | نوع "حفظ" له أولوية أعلى في جدولة المراجعة مقارنةً بالأنواع الأخرى |

### 5.3. المزامنة والعمل دون اتصال

| الكود | المتطلب |
|-------|---------|
| FR-M1 | تخزين البيانات محلياً في IndexedDB عبر Dexie.js، والعمل كاملاً دون إنترنت |
| FR-M2 | حل تعارض المزامنة مع Supabase يعتمد **"أعلى رقم نسخة يربح" (Revision-Number Based)**: كل سجل يحمل `version: int` يتصاعد محلياً مع كل تعديل، والنسخة ذات الرقم الأعلى تُعتمد في السحاب عند عودة الاتصال |

### 5.4. إدارة الوسائط والتخزين (Freeze Policy)

| الكود | المتطلب |
|-------|---------|
| FR-ST1 | رفع ملفات صوتية (حتى 5 دقائق) وصور للأغلفة مجاناً في الشهر الأول من أول رفع |
| FR-ST2 | بعد الشهر الأول بدون اشتراك Pro: تُجمَّد الملفات القديمة للقراءة فقط (تعمل ولا تُحذف) ويُحظر رفع ملفات جديدة |
| FR-ST3 | الوسائط مرتبطة بـ **ملاحظة** أو **محاضرة** (واحدة فقط لا كلاهما) |

### 5.5. مشاركة الفوائد

| الكود | المتطلب |
|-------|---------|
| FR-SH1 | تحويل أي ملاحظة إلى "عامة" عبر `is_public: boolean` |
| FR-SH2 | توليد رابط ويب عام وقابل للقراءة فقط (`/share/note_id`) |
| FR-SH3 | التقنية: سياسات Supabase RLS بدون تكاليف خارجية |

### 5.6. تصدير البيانات (Export)

| الكود | المتطلب |
|-------|---------|
| FR-EX1 | تصدير الملاحظات بصيغة **Markdown** |
| FR-EX2 | تصدير الملاحظات بصيغة **PDF** |
| FR-EX3 | التصدير متاح على مستوى ملاحظة واحدة أو قسم كامل |

---

## 6. أنواع الملاحظات (Note Types)

| النوع | الوصف | ملاحظة |
|-------|-------|--------|
| **فائدة** | معلومة علمية يريد الطالب حفظها | — |
| **قاعدة** | قاعدة فقهية أو أصولية | — |
| **سؤال** | إشكال يريد البحث عنه لاحقاً | — |
| **تعقيب** | ملاحظة نقدية أو إضافة على ما قرأ | — |
| **حفظ** | نص يريد استظهاره (آية، حديث، متن) | أولوية أعلى في SRS |

---

## 7. هيكل قاعدة البيانات (Supabase — PostgreSQL)

> **ملاحظة:** على كل الجداول القابلة للتعديل السبعة، يُفرَض حل تعارضات المزامنة (أعلى `version` يفوز) على مستوى قاعدة البيانات عبر مشغّل `BEFORE UPDATE` — أي كتابة بـ `version` لا تتجاوز الحالية تُرفض بخطأ `SYNC_CONFLICT|<server_version>` (ترحيل M5).

### 7.1. جدول `categories`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | |
| `user_id` | uuid (FK) | |
| `name` | text | اسم القسم |
| `icon` | text (nullable) | أيقونة اختيارية للتمييز البصري |
| `created_at` | timestamptz | |

### 7.2. جدول `books`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | |
| `user_id` | uuid (FK) | |
| `category_id` | uuid (FK) | القسم التابع له |
| `title` | text | عنوان الكتاب |
| `total_pages` | int | عدد الصفحات |
| `current_page` | int | صفحة التوقف الحالية |
| `last_opened_at` | timestamptz | لتحديد الخريطة المحلية |
| `version` | int (default 1) | رقم النسخة لحل التعارض |

### 7.3. جدول `lecturers`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | |
| `user_id` | uuid (FK) | |
| `book_id` | uuid (FK) | الكتاب التابع له |
| `name` | text | اسم الشيخ / المدرّس |

### 7.4. جدول `lectures`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | |
| `lecturer_id` | uuid (FK) | |
| `title` | text | عنوان المحاضرة |
| `duration_minutes` | int | المدة الزمنية |
| `is_completed` | boolean (default false) | هل اكتملت؟ |
| `version` | int (default 1) | رقم النسخة لحل التعارض |

### 7.5. جدول `notes`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | |
| `user_id` | uuid (FK) | |
| `book_id` | uuid (FK, nullable) | مرتبط بكتاب (اختياري) |
| `lecture_id` | uuid (FK, nullable) | مرتبط بمحاضرة (اختياري) |
| `title` | text | مستخرج تلقائياً من أول سطر في المحتوى |
| `content` | text | المحتوى الكامل (يدعم روابط `[[`) |
| `type` | enum | فائدة / قاعدة / سؤال / تعقيب / حفظ |
| `review_date` | date | تاريخ المراجعة التالية (SRS) |
| `is_public` | boolean (default false) | لإتاحة المشاركة العامة |
| `version` | int (default 1) | رقم النسخة لحل التعارض |

> **قاعدة:** `book_id` و`lecture_id` لا يمكن أن يكونا مملوءَين معاً — واحد فقط أو لا شيء.

### 7.6. جدول `note_links`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | |
| `source_note_id` | uuid (FK) | الملاحظة المصدر |
| `target_note_id` | uuid (FK) | الملاحظة الهدف |

> **قاعدة:** يُعاد بناء هذا الجدول بالكامل عند كل حفظ للملاحظة — يعكس النص مباشرة.

### 7.7. جدول `media`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | |
| `user_id` | uuid (FK) | |
| `note_id` | uuid (FK, nullable) | مرتبط بملاحظة |
| `lecture_id` | uuid (FK, nullable) | مرتبط بمحاضرة |
| `type` | enum | `audio` / `image` |
| `url` | text | رابط الملف في Supabase Storage |
| `created_at` | timestamptz | |

> **قاعدة:** `note_id` و`lecture_id` لا يمكن أن يكونا مملوءَين معاً.

### 7.8. جدول `users`
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | uuid (PK) | مطابق لـ `auth.users.id` في Supabase |
| `email` | text | البريد الإلكتروني |
| `display_name` | text | الاسم الظاهر |
| `created_at` | timestamptz | وقت التسجيل |
| `media_trial_started_at` | timestamptz (nullable) | تاريخ أول رفع وسائط |

---

## 8. نموذج التسعير

| الميزة | الخطة المجانية (طالب) | خطة المحترفين (وقف) |
|-------|-----------------------|---------------------|
| الميزات الأساسية (كتب، ملاحظات، خريطة، SRS) | غير محدودة | غير محدودة |
| المزامنة السحابية | كاملة وتلقائية | كاملة وتلقائية |
| رفع الوسائط (صوت / صور) | الشهر الأول مجاناً، ثم تجميد | مساحة سحابية مخصصة |
| تصدير البيانات (Markdown / PDF) | متاح | متاح |
| **السعر** | **0$ مدى الحياة** | **2.99$ شهرياً** |
| خيار الشراء مدى الحياة | — | TBD — يُحدد بعد MVP |

---

## 9. متطلبات النشر والتوافق

| المعيار | التفاصيل |
|---------|---------|
| **المنصة** | تطبيق ويب تقدمي نقي (Pure PWA) |
| **التوافق** | متجاوب بالكامل مع جميع أحجام الشاشات |
| **التثبيت** | قابل للتثبيت من Safari (iOS) وChrome (Android) |
| **Offline** | دعم كامل عبر Service Worker وIndexedDB |
| **قاعدة البيانات** | Supabase (PostgreSQL) |
| **التخزين المحلي** | Dexie.js فوق IndexedDB |
| **الوسائط** | Supabase Storage |
| **المصادقة** | Supabase Auth (Google OAuth + Email/Password) |

---

## 10. القرارات المؤجلة (Deferred Decisions)

| القرار | السبب |
|--------|-------|
| سعر خطة الـ Lifetime | يُحدد بعد قياس تفاعل المستخدمين مع المنتج |
| ربط Google Drive كبديل للتخزين | يُضاف لاحقاً كميزة Pro إذا طالب بها المستخدمون |

---

*نهاية الوثيقة — النسخة 2.0*
